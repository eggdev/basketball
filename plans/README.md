# Implementation Plans

Generated by the improve skill on 2026-09-18 at commit `009198a`. These plans
cover the selected valuation trust layer, usable-points valuation, and
scenario/mock-auction work. Execute them in the order below unless dependencies
say otherwise. Each executor must read its plan fully, honor its STOP
conditions, and update its row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Reconcile canonical player identities transactionally | P1 | M | — | DONE — reviewed at `da6932d`; 12 live merges audited |
| 002 | Persist and promote immutable auction valuation runs | P1 | M | 001 | DONE — approved at `6b87f3d`; SQL adapter lacks an integration harness |
| 003 | Import versioned NBA schedules and Fantrax playoff periods | P1 | M | 001 | DONE — approved at `0effe41`; live validation passed |
| 004 | Value usable daily-lineup production | P1 | L | 002, 003 | DONE — integrated at `cf1eb8d`; migration `0012` applied and `usable-lineup-v1` promoted |
| 005 | Make pre-draft scenarios selectable and comparable | P2 | M | — | TODO |
| 006 | Learn uncertainty-aware manager demand profiles | P2 | L | 001, 002 | TODO |
| 007 | Simulate saved auction scenarios | P2 | L | 004, 005, 006 | TODO |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` (with a one-line
reason), or `REJECTED` (with a one-line rationale).

## Dependency notes

- 001 lands before valuation persistence and demand modeling so every stored
  artifact references one durable player identity rather than preserving the
  current runtime name bridge.
- 002 establishes immutable, explicitly promoted valuation inputs before 004
  changes the production-value basis and before 006 uses walk-forward price
  residuals.
- 003 supplies dated NBA games and Fantrax playoff windows; 004 must not invent
  schedule counts or infer them from team abbreviations alone.
- 005 is independent and can run in parallel with 001–004, but it must land
  before 007 so simulations have explicit scenario inputs.
- 006 produces the smoothed opponent-demand distributions consumed by 007.
- 007 is last because it combines promoted values, usable-points outputs,
  selected plans, and manager profiles. It must report distributions rather
  than a single predicted draft.

## Deep-module seams

- Player reconciliation exposes preview and commit operations; table-by-table
  foreign-key rewrites remain hidden in the database implementation.
- Schedule ingestion exposes one versioned season calendar; provider paging,
  caching, and Fantrax/BALLDONTLIE adapters remain internal.
- Usable-points valuation exposes a board calculation and a roster-candidate
  calculation; daily assignment machinery stays internal.
- Auction simulation accepts one versioned input and returns one distributional
  result; bidding turns and random sampling stay internal.

## Findings considered and rejected

- Direct Fantrax bidding or automated waiver claims: deliberately deferred.
  Current provider data is incomplete and inferred in places; irreversible
  actions should wait until durable shadow-mode decisions have been evaluated.
- Treating mock-draft output as playoff probability: rejected because the
  current system does not simulate weekly opponents, injuries, transactions,
  or full-season lineup management. Plan 007 uses clearly labeled scenario
  outcomes instead.
- Adding ADP as a price feature immediately: deferred until enough correctly
  timestamped historical preseason snapshots exist for leakage-free testing.
